# Bad Request response - 400 error
description: Bad request
content:
  application/json:
    schema:
      $ref: '../../schemas/error/ErrorResponse.yaml'
    example:
      error: "validation_error"
      code: "invalid_parameter"
      message: "Bad request"
