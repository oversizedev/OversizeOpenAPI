description: List of features
content:
  application/json:
    schema:
      type: object
      description: List of features
      required:
        - features
      properties:
        features:
          type: array
          description: Collection of features
          items:
            $ref: '../../schemas/feature/Feature.yaml'