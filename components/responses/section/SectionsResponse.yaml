description: List of features sections
content:
  application/json:
    schema:
      type: object
      description: List of features features
      required:
        - mainSection
        - sections
      properties:
        mainSection:
          description: General section
          $ref: '../../schemas/feature/Section.yaml'
        sections:
          type: array
          description: Collection of sections
          items:
            $ref: '../../schemas/feature/Section.yaml'