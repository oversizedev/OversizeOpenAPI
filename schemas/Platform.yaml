type: object
required:
  - id
  - title
properties:
  id:
    type: string
    enum:
      - iOS
      - macOS
      - tvOS
      - watchOS
      - visionOS
      - web
    example: iOS
  title:
    type: string